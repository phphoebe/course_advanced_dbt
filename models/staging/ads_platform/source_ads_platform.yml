sources:
  - name: ads_platform
    schema: bingeflix
    database: raw
    tables:
      - name: daily_ads
        identifier: ads
        description: '{{ doc("bingeflix_source_table_daily_ads") }}'
